import 'package:flutter/material.dart';

class ChatScreenAI extends StatelessWidget {
  const ChatScreenAI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}