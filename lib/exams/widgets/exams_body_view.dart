import 'package:flutter/material.dart';
import 'package:graduation_project/exams/widgets/exams_page.dart';

class ExamsBodyView extends StatelessWidget {
  const ExamsBodyView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column (
      children: [
        // Expanded( // يجعل ExamsPage يأخذ كل المساحة المتاحة بشكل مرن
        //   // child: ExamsPage(),
        // ),
      ],
    );
  }
} 
